[package]
name = "nix-lint"
version = "0.1.0"
edition = "2024"

[dependencies]
nix-lint-core = { path = "../nix-lint-core" }
nix-lint-rules = { path = "../nix-lint-rules" }
clap = { version = "4.5", features = ["derive"] }
